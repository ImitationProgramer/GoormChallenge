#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n;
    char *word = (char *)malloc(sizeof(char) * n);
    scanf("%d", &n);
    scanf("%s", word);
    printf("%d", 5 * n + 4);
    return 0;
}